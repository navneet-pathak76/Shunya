# SUNYA Theme

The visual system is dark-first, glassmorphism-oriented, and driven by centralized design tokens. Accent colour is intentionally configurable so a future settings screen can change the brand accent without rewriting feature UI.
